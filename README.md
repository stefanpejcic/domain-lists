# domain-lists

## Project structure

```
domain-lists/
├── .gitignore
├── README.md
├── install.sh
├── daily.sh
├── keywords.json      # tracked keywords for /trending (edit, no code change needed)
├── tailwind.config.js
├── scripts/
│   ├── download_zones.py
│   ├── process_zones.py
│   ├── generate_distribution.py
│   ├── extract_short_domains.py
│   ├── tld_scrapper.py
│   ├── notify.py                  # sends a failure alert email (reads EMAIL from website/.env)
│   ├── generate_keyword_lists.py  # filters today's new/all.txt.gz by keywords.json
│   ├── generate_threat_matches.py # cross-references today's new domains against OpenPhish/URLhaus
│   ├── generate_live_domains.py   # checks today's trending domains against the CT logs API
│   ├── generate_sitemap.py        # writes sitemap.xml from json/ (needs SITE_URL in website/.env)
│   ├── generate_og_images.py      # writes website/static/og/*.png from sitemap.xml
│   └── build_css.sh               # compiles website/static/css/style.css (Tailwind, standalone CLI)
└── website/
    ├── app.py           # also exposes /api/v1/* (JSON/CSV, rate-limited)
    ├── .env              # EMAIL=..., SITE_URL=... (gitignored)
    ├── static/
    │   ├── css/           # input.css (tracked) + style.css (generated, gitignored)
    │   └── og/            # generated OG images (gitignored)
    └── templates/
        ├── base.html          # shared header/footer/layout + SEO meta
        ├── index.html
        ├── deleted_browse.html
        ├── tld_browse.html
        ├── dropped.html        # combined "deleted today" browse page
        ├── trending.html       # keyword-filtered "new today" browse page
        ├── live.html           # trending domains confirmed live via CT logs
        ├── threats.html        # new domains matched against threat intel feeds
        └── patterns.html
```

### API

`/api/v1` lists available endpoints. Unlike the browser download flow (which
gates files behind a captcha/countdown and single-use tokens), the API is
meant for scripts: no token dance, just a 60 requests/minute per-IP limit.

- `GET /api/v1/summary` — global totals
- `GET /api/v1/tlds` (or `?format=csv`) — stats for every TLD
- `GET /api/v1/tld/<tld>` — stats for one TLD
- `GET /api/v1/tld/<tld>/<kind>` — direct `.txt.gz` download (`kind` is
  `domains`, `new`, or `deleted`; `tld=all` combines every TLD)

### Trending keyword lists

`/trending` and `/trending/<keyword>` browse domains registered today that
match a tracked keyword (e.g. "ai", "crypto"). Generated by
`scripts/generate_keyword_lists.py`, which filters `lists/<date>/new/all.txt.gz`
by substring match against the domain name with its TLD stripped off (so
`.ai` domains don't auto-match "ai"). It's a discovery tool, not a precision
filter — expect some false positives (e.g. "domain" contains "ai"). Run it
after `process_zones.py`; edit `keywords.json` at the repo root to change
which keywords are tracked, no code changes needed.

### Threat matches

`/threats` and `/threats/<source>` browse domains registered today that are
already flagged by a public threat intel feed (OpenPhish, URLhaus — both
free, no API key). Generated by `scripts/generate_threat_matches.py`, which
downloads each feed, extracts hostnames from the listed URLs, and walks each
hostname's dot-suffixes against today's `new/all.txt.gz` domain set to find
matches — no public-suffix-list needed, since a match is only reported if
the exact suffix is itself a domain we tracked as newly registered. Results
are leads to investigate, not verified verdicts — third-party feeds can have
false positives. Run it after `process_zones.py`.

### Live domains (Certificate Transparency)

`/live` and `/live/<keyword>` browse today's trending keyword domains that
already have a live HTTPS certificate — a much stronger signal than
registration alone, since most newly registered domains sit dormant for
weeks. Generated by `scripts/generate_live_domains.py`, which checks each
domain from `lists/<date>/keywords/` (produced by `generate_keyword_lists.py`)
against a self-hosted Certificate Transparency API's `GET /domain/<name>`
endpoint — one request per candidate domain, scoped to the trending lists
(a few hundred/day) rather than every new domain (hundreds of thousands/day)
to keep that request count reasonable. Requires `CT_LOGS` in `website/.env`
pointing at the API's base URL. Run it after `generate_keyword_lists.py`.

### Failure alerts

`cron.sh` checks the exit code of `download_zones.py` and `process_zones.py`
and, on failure, calls `scripts/notify.py` to email the tail of the relevant
log via the local `sendmail`. The recipient is read from `EMAIL` in
`website/.env` (falls back to `stefan@pejcic.rs` if unset).

All paths in `scripts/` and `website/app.py` are resolved relative to the
repo root, so the app works regardless of where it's installed/cloned.

### Created at runtime (gitignored)

These are generated by the scripts/cron job and are not tracked in git:

```
downloads/            # raw zone files, downloaded daily (removed after processing)
lists/<date>/...      # generated domains / new / deleted / keywords / threats / live lists per TLD
json/...              # per-TLD stats, summary.json, pattern_counters.json, keywords.json, threats.json, live.json
venv/                  # Python virtualenv, created by install.sh
*_log.txt              # script run logs (icann_download_log.txt, zone_processing_log.txt, ...)
sitemap.xml             # written by scripts/generate_sitemap.py
website/static/css/style.css  # written by scripts/build_css.sh
website/static/og/            # written by scripts/generate_og_images.py
```
